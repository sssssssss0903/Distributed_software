spring:
  application:
    name: seckill-product-service
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        namespace: dev
      config:
        server-addr: localhost:8848
        namespace: dev
        file-extension: yaml
        shared-configs:
          - data-id: seckill-common.yaml
            group: DEFAULT_GROUP
            refresh: true
