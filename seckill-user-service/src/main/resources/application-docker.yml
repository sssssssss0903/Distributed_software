# Docker 容器环境配置 - 使用服务名连接
spring:
  datasource:
    url: jdbc:mysql://mysql:3306/seckill_user?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: root

  redis:
    host: redis
    port: 6379

  # Docker 环境禁用 Nacos
  cloud:
    nacos:
      discovery:
        enabled: false
      config:
        enabled: false
